export class Lift {
  constructor(id, floor, status, htmlElement) {
    this.id = id;
    this.floor = floor;
    this.status = status;
    this.htmlElement = htmlElement;
  }

  moveLift(toFloor) {}
}
